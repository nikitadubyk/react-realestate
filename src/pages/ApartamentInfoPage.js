import MoreInfo from '../components/more-info/MoreInfo';
import Header from '../components/header/Header';

const ApartamentInfoPage = () => {
    return (
        <div>
            <Header />
            <MoreInfo />
        </div>
    );
};

export default ApartamentInfoPage;
